# vLLM connection (Vast AI template: internal port 18000)
VLLM_MODEL=Qwen/Qwen3.5-4B
VLLM_BASE_URL=http://localhost:18000/v1
VLLM_API_KEY=dummy

# Pipeline version — which physics_solution version to load
PIPELINE_VERSION=v05_best_vLLM

# Per-question timeout (seconds). Server reserves 2s margin.
QUESTION_TIMEOUT_S=60.0

# LangSmith tracing (optional — leave blank to disable)
LANGSMITH_API_KEY=
