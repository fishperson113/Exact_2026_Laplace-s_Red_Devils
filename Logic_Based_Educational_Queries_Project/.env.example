# Sao chép thành `.env` (cùng cấp `configs/`) — KHÔNG commit `.env`.
#
# Repo này chỉ dùng `.env` cho **bí mật**. Mọi siêu tham số train/eval/Hub nằm trong Git:
#   - `configs/logic_model.yaml` — Logic / MCQ SFT
#   - `configs/fol_model.yaml` — FOL SFT
#
# Thư viện Hugging Face cũng đọc `HUGGINGFACE_HUB_TOKEN` nếu bạn đặt tên đó thay cho `HF_TOKEN`
# (chỉ cần một trong hai, không bắt buộc cả hai).

HF_TOKEN=
GIT_TOKEN=
