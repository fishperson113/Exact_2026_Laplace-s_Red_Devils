---
source: Giancoli Physics: Principles with Applications, 7th ed.
chapter: 21
problem_number: 67
answer_source: Answers to Odd-Numbered Problems
needs_review: true
---

# Problem 21.67

## Question
(III) (a) What is the rms current in an RC circuit if R = 6.60 k(cid:3), C = 1.80 mF, and the rms applied voltage is 120 V at 60.0 Hz? (b) What is the phase angle between voltage and current? (c) What are the voltmeter readings across R and C? R = 520 (cid:3).

## Final answer from source
(a) (b) (c)

## Worked solution
The problem statement appears to have OCR corruption/incompleteness:

- `k(cid:3)` is likely intended to be `kΩ`
- `mF` may be intended as `µF` or `mF`, but this is ambiguous from the text alone
- The trailing `R = 520 (cid:3)` is also unclear and may indicate a missing part of the question or a formatting/OCR error
- The source final answer is not provided numerically; it only shows `(a) (b) (c)`

Because the numerical source answer is missing and one value appears corrupted, a fully source-constrained worked solution cannot be completed reliably. However, the correct RC-circuit method would be:

1. Compute the capacitive reactance:
   \[
   X_C = \frac{1}{2\pi f C}
   \]

2. Compute the impedance:
   \[
   Z = \sqrt{R^2 + X_C^2}
   \]

3. Find the rms current:
   \[
   I_{\text{rms}} = \frac{V_{\text{rms}}}{Z}
   \]

4. Find the phase angle:
   \[
   \tan \phi = \frac{X_C}{R}
   \]
   In a series RC circuit, current leads the applied voltage by this angle.

5. Voltmeter readings across the resistor and capacitor:
   \[
   V_R = I_{\text{rms}} R,\qquad V_C = I_{\text{rms}} X_C
   \]

If the intended values are indeed \(R=6.60\text{ k}\Omega\), \(C=1.80\text{ mF}\), \(V_{\text{rms}}=120\text{ V}\), and \(f=60.0\text{ Hz}\), then the above formulas should be evaluated numerically. But since the source answer is not present and the problem text seems corrupted, I cannot guarantee a source-faithful numerical result.

## Key concepts used
- Series RC circuit impedance
- Capacitive reactance \(X_C = 1/(2\pi f C)\)
- RMS current \(I_{\text{rms}} = V_{\text{rms}}/Z\)
- Phase angle in RC circuits
- Component voltage drops: \(V_R = IR\), \(V_C = IX_C\)
