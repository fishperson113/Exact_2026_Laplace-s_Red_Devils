# .env.example
# Copy file này thành .env rồi điền giá trị thực:
#   cp .env.example .env

# HuggingFace token — lấy tại https://huggingface.co/settings/tokens
# Cần quyền READ để tải model từ Hub (đặc biệt model private)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
