# ── Core inference (pinned to Colab-verified versions) ─────
torch==2.8.0
transformers==5.9.0
accelerate==1.10.1
safetensors
sentencepiece==0.2.1
tokenizers==0.22.1

# ── Prompt / chain composition ─────────────────────────────
langchain-core==0.3.78

# ── Data & utils ───────────────────────────────────────────
pandas==2.2.2
numpy==2.0.2
tqdm==4.67.1
pyyaml==6.0.3
python-dotenv==1.1.1

# ── HuggingFace hub ───────────────────────────────────────
huggingface_hub==1.16.1

# ── Tracing (optional — no-op if LANGSMITH_API_KEY unset) ──
langsmith==0.4.33

# ── Fast attention kernels for Qwen3.5 ────────────────────
flash-linear-attention
# causal-conv1d — requires pre-built wheel matched to torch+cuda+python.
#   Installed automatically by: install_fast_kernels()
#   Or manually via GitHub release wheel (see README_GPU_SETUP.md).
