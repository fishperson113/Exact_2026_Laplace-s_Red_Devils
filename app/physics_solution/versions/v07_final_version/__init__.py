"""v07 final version — importable code package (build_sft, train, eval).

The doc/data folder was renamed from ``v07_final-version`` (hyphen, not
importable) to ``v07_final_version`` (underscore) so this same folder holds both
the authoritative README/data AND the runnable modules.
"""
