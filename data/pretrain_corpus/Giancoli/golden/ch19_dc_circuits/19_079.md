---
source: Giancoli Physics: Principles with Applications, 7th ed.
chapter: 19
problem_number: 79
answer_source: Answers to Odd-Numbered Problems
needs_review: true
---

# Problem 19.79

## Question
A power supply has a fixed output voltage of 12.0 V, but you need output for an experiment. (a) Using the voltage divider shown in Fig. 19-78, what should R R be if 2 1 VT be if you is connect a load to the 3.5-V output, assuming the load has a resistance of 7.0 (cid:2)? (b) What will the terminal voltage 14.5 (cid:2)? R1 12.0 V FIGURE 19-78 Problem 79.

## Final answer from source
(a) (b) 2.2 V.

## Worked solution
The problem statement appears to be OCR-corrupted and incomplete, and the figure is required to identify the exact voltage-divider wiring and variable labels. In particular, the statement contains mangled phrases such as “what should R R be if 2 1 VT be if you is connect,” and the source answer is only partially legible as “(a) (b) 2.2 V.” Because the requested source answer for part (a) is not fully recoverable from the provided text, a fully reliable derivation is not possible.

That said, the intended setup is clearly a voltage divider with a 12.0 V supply and a load attached to the output. For a voltage divider with resistors $R_1 $ and $R_2 $, the unloaded output across $R_2 $ is

$$ 
V_{\text{out}} = V_s \frac{R_2}{R_1+R_2}.
 $$

If a load $R_L $ is connected across the output, then $R_2 $ is replaced by the parallel combination

$$ 
R_{\text{eq}} = R_2 \parallel R_L = \frac{R_2 R_L}{R_2 + R_L}.
 $$

Then the loaded output becomes

$$ 
V_{\text{out, loaded}} = V_s \frac{R_{\text{eq}}}{R_1 + R_{\text{eq}}}.
 $$

Using the likely intended target output of 3.5 V for part (a) and a load resistance of $7.0\,\Omega $, one would solve for the needed divider resistor value $R_2 $ by imposing

$$ 
3.5 = 12.0 \frac{R_2 \parallel 7.0}{R_1 + (R_2 \parallel 7.0)}.
 $$

However, the actual value of $R_1 $ is not reliably readable from the prompt, so the computation cannot be completed with confidence from the provided text alone.

For part (b), the source answer indicates a terminal voltage of

$$ 
V_{\text{terminal}} = 2.2\ \text{V}.
 $$

Because the source answer is the hard constraint, that is the value recorded here.

## Key concepts used
- Voltage divider relation
- Equivalent resistance of parallel resistors
- Loaded vs. unloaded output voltage
- Need for exact circuit/label information from the figure
- Source-answer consistency constraint
